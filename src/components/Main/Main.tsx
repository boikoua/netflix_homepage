import AboutMovie from '../AboutMovie';
import styles from './Main.module.scss';

const Main = () => {
  return (
    <main className={styles.main}>
      <AboutMovie />
    </main>
  );
};

export default Main;
